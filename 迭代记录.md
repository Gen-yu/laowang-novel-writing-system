# 迭代记录

> 记录项目的每次变更，最新版本在最前。每次修改代码后请同步更新本文档（见 [修改说明.md](修改说明.md)）。

---

## v1.2.1（2026-08-15）· 收紧术语维护规则

### 调整

- `chapter-summary` 伪 skill 将章节总结与术语表职责严格分离，章节标题、章节日志和剧情复盘不再进入术语文档。
- 术语表改为“宁缺毋滥”：人物只保留稳定身份、关系、长期特征及极简必要事实；组织、地点、环境、物品等只收录本书特有且后续写作需要依赖的设定。
- 明确禁止在术语表中设置“规则”和“关键事件”栏目，并排除常识、普通实体、一般动作及临时状态。
- 本章没有可靠新术语或明确修正时，要求完整原样返回已有术语，不再为产生更新而改写或扩充内容。

### 文档

- 同步更新 [基本需求.md](基本需求.md)、[README.md](README.md)、[技术文档.md](技术文档.md) 和本文档中的术语维护约定。

### 验证说明

- 本次仅修改纯文本伪 skill 与文档，未修改 JavaScript，也未调用外部 AI 接口；实际模型遵循效果需在本地总结章节时验证。

---

## v1.2.0（2026-08-11）· 多书籍、伪 skill 与分卷总结

### 新增

- `RAG/` 一级子目录现在代表独立书籍；页面顶部增加“当前书籍”选择器和“新建书籍”按钮。
- 新建书籍时自动创建 `characters/`、`plots/volumes/`、`plots/chapter-bodies/`、`plots/chapter-summaries/`、`settings/`。
- 写作状态改为 `data/writing/<书名>.json`，切换书籍前保存当前状态，切换后读取目标书籍的正文、草稿、章节名和召回编辑事务。
- 章节归档由服务器自动分配下一章节号；标题留空生成 `第N章`，填写标题生成 `第N章：标题`。扫描兼容阿拉伯数字和旧版中文数字章节名。
- 新增 `chapter-summary` 伪 skill。总结本章时先保存正文，再读取已有术语，并生成章节总结及完整更新版术语。
- 新增资料页“分卷总结”弹窗和 `volume-summary` 伪 skill；支持选择下一卷号和连续章节，读取历史卷与本卷章节总结后生成卷总结。
- 新增书籍存储服务 [server/services/bookStorage.js](server/services/bookStorage.js)，集中处理目录初始化、书名和路径校验、编号解析、原子写入及旧数据迁移。
- 新增书籍列表/创建、卷元数据/生成接口，RAG 文件和写作状态接口全部增加书籍作用域。

### 调整

- 原单书资料迁移至 `RAG/example/`，原 `data/writing.json` 迁移至 `data/writing/example.json`。
- 旧 `plots/chapters/` 合并为章节总结；同名不同内容使用“迁移冲突”后缀保留，不静默覆盖。
- 旧版 `第一卷.md` 等中文卷号文件参与下一卷号计算并作为历史上下文读取；没有章节范围的旧卷不参与章节范围重叠判断。
- `writing-hint`、`writing-complete`、`image-prompt-optimize` 的内置提示词迁入各自 `SKILL.md`。
- 伪 skill 文件缺失或正文为空时直接返回配置错误，不再回退服务器默认提示词。
- 章节总结模型输出采用 `【章节总结】` / `【更新后术语】` 纯文本协议；解析失败时保留正文且不覆盖原术语。
- 资料 API 使用书内相对路径并进行目录边界校验，禁止跨书或访问项目其他文件。

### 文档

- 更新 [基本需求.md](基本需求.md)、[README.md](README.md)、[技术文档.md](技术文档.md) 和本文档，记录多书籍目录、自动编号、伪 skill、术语更新、分卷总结、迁移及接口约定。

### 验证说明

- 对修改过的服务器及浏览器 JavaScript 执行 `node --check`。
- 静态核对迁移后的 `RAG/example/`、`data/writing/example.json`、路由字段和旧引用。
- 本次未调用外部 AI 或生图接口，实际模型输出效果与浏览器交互仍建议由使用者在本地服务中验证。

---

## v1.1.0（2026-08-08）· 章节双重归档与原位编辑

### 新增

- 点击「总结本章」时，先将当前定稿正文写入 `RAG/plots/chapter-bodies/<章节名>.md`，再生成总结并写入 `RAG/plots/chapter-summaries/<章节名>.md`。
- 总结接口返回正文与总结两个相对路径；即使 AI 总结调用失败，已经归档的章节正文仍会保留。
- 写作状态新增 `activeRecall`，持久化召回前的段落快照、段落选区坐标和原编辑区草稿，刷新页面后仍可继续完成原位替换。

### 调整

- 原 `RAG/plots/chapters/` 更名为 `RAG/plots/chapter-summaries/`，其中已有的 `第一章.md`、`测试章.md`、`未命名章节.md` 已迁移；新增 `RAG/plots/chapter-bodies/` 用于章节正文。
- 资料页目录标签同步调整为“章节正文”和“章节总结”。
- 阅读区划选内容放回编辑区后，不再立即删除阅读区原文；确认按钮切换为「确认替换」，修改内容按起止段落索引和段内偏移替换回原位置。
- 召回编辑前若编辑区已有草稿，会先暂存该草稿，完成替换后自动恢复；普通「确认归稿」仍追加到阅读区末尾。

### 文档

- 更新 [README.md](README.md) 和 [技术文档.md](技术文档.md)，补充双目录归档、召回编辑事务、刷新恢复和原位替换说明。

### 验证说明

- 按本次修改要求未启动应用、未运行接口或自动化测试，仅进行了代码和目录引用的静态检查。

---

## v1.0.2（2026-08-07）· 品牌图标适配

### 调整

- 扩大顶部品牌印章图标的宽度并增加横向内边距，使“老王”等双字名称能够完整显示；同步调整字号、字间距与旋转角度，保持朱砂印章风格。

---

## v1.0.1（2026-08-07）· 测试数据修正

### 修复

- 修正旧版 `data/writing.json` 中由早期命令行编码问题产生的中文乱码，恢复为可读的测试段落、草稿和章节名。

---

## v1.0.0（2026-08-07）· 初始版本

按 [基本需求.md](基本需求.md) 完成项目首次生成。

### 新增

**整体**
- Node.js + Express 后端 + 原生 HTML/CSS/JS 单页前端（无构建步骤）。
- 顶部导航栏切换三个功能页：写作 / 资料 / 生图。
- 纸质阅读风格 UI：宣纸底色、墨色文字、朱砂强调色、衬线字体。

**后端**（[server/](server/)）
- [server/index.js](server/index.js)：服务器入口，静态托管 `public/` + 挂载 API 路由。
- [server/config.js](server/config.js)：集中读取 `.env`（接口地址、密钥、模型、端口、目录常量）。
- [server/routes/ai.js](server/routes/ai.js)：AI 提示、AI 补全和本章总结。
- [server/routes/rag.js](server/routes/rag.js)：RAG 目录树、文件读写，含路径穿越防护。
- [server/routes/image.js](server/routes/image.js)：提示词优化和图片生成。
- [server/routes/writing.js](server/routes/writing.js)：写作状态持久化。
- [server/services/llm.js](server/services/llm.js)：写作模型客户端（Chat Completions）。
- [server/services/imageService.js](server/services/imageService.js)：生图客户端，兼容 b64_json / url 两种返回，图片存入 `public/generated/`。

**前端**（[public/](public/)）
- [public/index.html](public/index.html)：单页骨架（导航栏 + 三个功能页）。
- [public/css/style.css](public/css/style.css)：纸质阅读风格样式。
- [public/js/writing.js](public/js/writing.js)：写作页——左阅读区（定稿只读）+ 右编辑区（读写）。
- [public/js/resources.js](public/js/resources.js)：资料页资源树和文件编辑。
- [public/js/image.js](public/js/image.js)：生图页提示词优化与生成。
- [public/js/app.js](public/js/app.js)：导航切换和通用请求工具。

### 修复

- 初版跨段落划选偏移不准确，后续改为按段落索引与段内偏移处理。
- 段落为空时不响应划选，避免占位提示文字进入编辑区。

### 测试结果（2026-08-07）

| 项目 | 结果 |
| --- | --- |
| 首页 / 静态资源（css、js） | ✅ 200 |
| 写作状态读写 | ✅ |
| RAG 目录树 / 文件读写 | ✅ |
| 路径穿越防护 | ✅ 已拦截 |
| AI 提示 / AI 补全 / 总结本章 / 提示词优化 | ✅ 接口联通，中文输出正常 |
| 图片生成 | ⚠️ 链路正常，生图模型账户余额不足（非代码问题） |
