# 迭代记录

> 记录项目的每次变更，最新版本在最前。每次修改代码后请同步更新本文档（见 [修改说明.md](修改说明.md)）。

---

## v1.1.0（2026-08-08）· 章节双重归档与原位编辑

### 新增

- 点击「总结本章」时，先将当前定稿正文写入 `RAG/plots/chapter-bodies/<章节名>.md`，再生成总结并写入 `RAG/plots/chapter-summaries/<章节名>.md`。
- 总结接口返回正文与总结两个相对路径；即使 AI 总结调用失败，已经归档的章节正文仍会保留。
- 写作状态新增 `activeRecall`，持久化召回前的段落快照、段落选区坐标和原编辑区草稿，刷新页面后仍可继续完成原位替换。

### 调整

- 原 `RAG/plots/chapters/` 更名为 `RAG/plots/chapter-summaries/`，其中已有的 `第一章.md`、`测试章.md`、`未命名章节.md` 已迁移；新增 `RAG/plots/chapter-bodies/` 用于章节正文。
- 资料页目录标签同步调整为“章节正文”和“章节总结”。
- 阅读区划选内容放回编辑区后，不再立即删除阅读区原文；确认按钮切换为「确认替换」，修改内容按起止段落索引和段内偏移替换回原位置。
- 召回编辑前若编辑区已有草稿，会先暂存该草稿，完成替换后自动恢复；普通「确认归稿」仍追加到阅读区末尾。

### 文档

- 更新 [readme.md](readme.md) 和 [技术文档.md](技术文档.md)，补充双目录归档、召回编辑事务、刷新恢复和原位替换说明。

### 验证说明

- 按本次修改要求未启动应用、未运行接口或自动化测试，仅进行了代码和目录引用的静态检查。

---

## v1.0.2（2026-08-07）· 品牌图标适配

### 调整

- 扩大顶部品牌印章图标的宽度并增加横向内边距，使“老王”等双字名称能够完整显示；同步调整字号、字间距与旋转角度，保持朱砂印章风格。

---

## v1.0.1（2026-08-07）· 测试数据修正

### 修复

- 修正 [data/writing.json](data/writing.json) 中由早期命令行编码问题产生的中文乱码，恢复为可读的测试段落、草稿和章节名。

---

## v1.0.0（2026-08-07）· 初始版本

按 [需求.md](需求.md) 完成项目首次生成。

### 新增

**整体**
- Node.js + Express 后端 + 原生 HTML/CSS/JS 单页前端（无构建步骤）。
- 顶部导航栏切换三个功能页：写作 / 资料 / 生图。
- 纸质阅读风格 UI：宣纸底色、墨色文字、朱砂强调色、衬线字体。

**后端**（[server/](server/)）
- [server/index.js](server/index.js)：服务器入口，静态托管 `public/` + 挂载 API 路由。
- [server/config.js](server/config.js)：集中读取 `.env`（接口地址、密钥、模型、端口、目录常量）。
- [server/routes/ai.js](server/routes/ai.js)：AI 提示 `/api/ai/hint`、AI 补全 `/api/ai/complete`、本章总结 `/api/ai/summarize`（自动存档至 `RAG/plots/chapters/`）。
- [server/routes/rag.js](server/routes/rag.js)：RAG 目录树、文件读写，含路径穿越防护。
- [server/routes/image.js](server/routes/image.js)：提示词优化 `/api/image/optimize`、图片生成 `/api/image/generate`。
- [server/routes/writing.js](server/routes/writing.js)：写作状态持久化（`data/writing.json`）。
- [server/services/llm.js](server/services/llm.js)：写作模型客户端（Chat Completions）。
- [server/services/imageService.js](server/services/imageService.js)：生图客户端，兼容 b64_json / url 两种返回，图片存入 `public/generated/`。
- [server/services/skillLoader.js](server/services/skillLoader.js)：skill 加载器，SKILL.md 正文为空时回退内置默认提示词。

**前端**（[public/](public/)）
- [public/index.html](public/index.html)：单页骨架（导航栏 + 三个功能页）。
- [public/css/style.css](public/css/style.css)：纸质阅读风格样式。
- [public/js/writing.js](public/js/writing.js)：写作页——左阅读区（定稿只读）+ 右编辑区（读写）；确认归稿、AI 提示、AI 补全、总结本章；鼠标划选阅读区文字可放回编辑区；草稿自动保存。
- [public/js/resources.js](public/js/resources.js)：资料页——左侧 RAG 资源管理器（树形目录），右侧查看 / 编辑 / 保存 / 新建文件。
- [public/js/image.js](public/js/image.js)：生图页——提示词输入 → 优化提示词 → 生成图片并展示。
- [public/js/app.js](public/js/app.js)：导航切换 + 通用工具（api / toast / withLoading）。

**Skill 占位文件**（[skills/](skills/)，按需求留空，仅创建文件）
- `skills/writing-hint/SKILL.md` —— AI 提示
- `skills/writing-complete/SKILL.md` —— AI 补全
- `skills/image-prompt-optimize/SKILL.md` —— 图片提示词优化

**资料库示例**（[RAG/](RAG/)）
- `characters/示例角色.md`、`plots/volumes/第一卷.md`、`plots/chapters/第一章.md`、`settings/术语.md`

**文档**
- [readme.md](readme.md)：功能与使用说明。

### 修复

- **划选放回编辑区的文字错位**（[public/js/writing.js:194](public/js/writing.js#L194)）：初版用 `paragraphs.join('\n\n')` 计算选区偏移，而阅读区 DOM 文本中段间无分隔符，跨段落划选会删错文字。改为新增 `removeRangeFromParagraphs()`，按段落长度累计偏移精确删除，跨段选中的剩余首尾自动合并为一段。
- **空阅读区误响应划选**（[public/js/writing.js:128](public/js/writing.js#L128)）：段落为空时不再响应划选，避免占位提示文字被移入编辑区。

### 测试结果（2026-08-07）

| 项目 | 结果 |
| --- | --- |
| 首页 / 静态资源（css、js） | ✅ 200 |
| 写作状态读写 `/api/writing` | ✅ |
| RAG 目录树 / 文件读写 `/api/rag/*` | ✅ |
| 路径穿越防护（请求 `../.env`） | ✅ 已拦截 |
| AI 提示 / AI 补全 / 总结本章 / 提示词优化 | ✅ 接口联通，中文输出正常 |
| 图片生成 `/api/image/generate` | ⚠️ 链路正常，生图模型账户余额不足（`Insufficient account balance`，非代码问题） |

### 已知事项

- 生图模型（`.env` 中 `IMAGE_MODEL`）账户余额不足，充值后即可使用；提示词优化不受影响（走写作模型）。
- curl 命令行直接内联中文 JSON 会因 Windows 控制台编码产生乱码，属测试工具问题；浏览器前端 fetch 默认 UTF-8，无此问题。命令行测试请用 `--data-binary @文件` 方式发送。
