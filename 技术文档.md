# 技术文档

> 本文仅介绍核心结构、功能调用链和常用修改位置，便于后续维护。功能与使用方法见 [readme.md](readme.md)，历史变化见 [迭代记录.md](迭代记录.md)。

## 1. 技术结构

项目采用简单的前后端结构，无前端构建步骤：

- **后端**：Node.js + Express
- **前端**：原生 HTML、CSS、JavaScript
- **AI 接口**：兼容 Chat Completions 与 Images Generations
- **资料存储**：RAG 目录下的 Markdown 文件
- **写作状态**：data/writing.json
- **生成图片**：public/generated/

主要目录：

```text
server/               后端服务
  routes/             API 路由
  services/           AI、图片与 skill 服务
public/               前端页面和静态资源
skills/               AI 功能的提示词 skill
RAG/                  人物、剧情、设定资料
```

## 2. 服务入口与配置

### 服务器入口

文件：[server/index.js](server/index.js)

主要职责：

1. 创建 Express 服务；
2. 托管 public 目录；
3. 挂载 `/api/ai`、`/api/image`、`/api/rag`、`/api/writing` 路由；
4. 监听 `.env` 中的端口。

新增一类后端功能时，通常先在 `server/routes/` 创建路由文件，再在此处挂载。

### 环境配置

文件：[server/config.js](server/config.js)

负责读取 `.env`，提供：

- `API_BASE_URL`：AI 接口地址；
- `WRITING_API_KEY`、`WRITING_MODEL`：写作模型；
- `IMAGE_API_KEY`、`IMAGE_MODEL`：生图模型；
- `RAG_DIR`、`SKILLS_DIR` 等目录常量。

更换模型或接口时，优先修改 `.env`，一般不需要修改业务代码。

## 3. 写作功能

### 前端

文件：[public/js/writing.js](public/js/writing.js)

核心状态：

```js
const writingState = {
  paragraphs: [], // 阅读区已定稿段落
  draft: '',      // 编辑区草稿或正在修改的召回内容
  chapter: '',    // 当前章节名
  activeRecall: null // 进行中的原位编辑事务
};
```

主要功能：

- `renderReading()`：渲染阅读区；
- `saveWriting()`：保存写作状态；
- 「确认归稿」：普通草稿追加到 `paragraphs` 末尾；
- 「确认替换」：召回编辑时根据段落索引和段内偏移替换回原位置；
- 「AI 提示」：请求 `/api/ai/hint`；
- 「AI 补全」：请求 `/api/ai/complete`，结果写回编辑区；
- 「总结本章」：请求 `/api/ai/summarize`，同时归档章节正文和章节总结；
- `getSelectionRange()`：把浏览器 DOM 选区转换为段落坐标；
- `replaceRangeInParagraphs()`：完成单段或跨段的原位替换。

如需调整写作页布局或按钮，修改 [public/index.html](public/index.html) 和 [public/css/style.css](public/css/style.css)。

### 后端

文件：[server/routes/ai.js](server/routes/ai.js)

| 接口 | 用途 |
| --- | --- |
| `POST /api/ai/hint` | 根据正文与草稿生成写作提示 |
| `POST /api/ai/complete` | 把编辑区大纲扩写成正式正文 |
| `POST /api/ai/summarize` | 先归档正文，再生成总结；分别写入 `RAG/plots/chapter-bodies/` 与 `RAG/plots/chapter-summaries/` |

AI 请求最终由 [server/services/llm.js](server/services/llm.js) 中的 `chat(system, user)` 发送。

写作状态的读取和保存位于 [server/routes/writing.js](server/routes/writing.js)，数据存储在 `data/writing.json`。`activeRecall` 会持久化召回前的段落快照、选区段落坐标和原编辑区草稿，使页面刷新后仍能把修改内容替换回原位置；旧状态文件缺少该字段时按 `null` 处理。

召回编辑流程：

```text
阅读区划选文字
  → 记录起止段落索引与段内偏移
  → 保存原段落快照及原草稿到 activeRecall
  → 编辑区显示选中文字（阅读区正文暂不改变）
  → 用户点击「确认替换」
  → replaceRangeInParagraphs() 按原坐标替换
  → 恢复召回前草稿并清空 activeRecall
```

总结接口会先写入章节正文，再请求 AI 并写入章节总结。因此 AI 总结失败时，正文文件仍会保留。接口成功返回 `summary`、`bodyPath` 和 `summaryPath`。

## 4. Skill 机制

文件：[server/services/skillLoader.js](server/services/skillLoader.js)

调用流程：

```text
业务路由
  → resolvePrompt(skill 名称, 默认提示词)
  → 读取 skills/<名称>/SKILL.md
  → 正文非空：使用 skill 正文
  → 正文为空：使用代码内置默认提示词
```

当前 skill：

| 功能 | 文件 |
| --- | --- |
| AI 写作提示 | skills/writing-hint/SKILL.md |
| AI 正文补全 | skills/writing-complete/SKILL.md |
| 生图提示词优化 | skills/image-prompt-optimize/SKILL.md |

后续调整 AI 行为时，优先填写对应 `SKILL.md` 的正文，不必修改接口代码。若要增加新 skill，可复制现有目录结构，并在业务路由中调用 `resolvePrompt()`。

## 5. 资料管理

### 后端

文件：[server/routes/rag.js](server/routes/rag.js)

| 接口 | 用途 |
| --- | --- |
| `GET /api/rag/tree` | 获取 RAG 目录树 |
| `GET /api/rag/file?path=...` | 读取资料文件 |
| `POST /api/rag/file` | 新建或保存资料文件 |

`safeResolve()` 会检查最终路径必须位于 RAG 目录内，修改文件逻辑时不要移除该检查。

资料分类：

```text
RAG/characters/                 人物设定
RAG/plots/volumes/              分卷剧情
RAG/plots/chapter-bodies/       章节正文
RAG/plots/chapter-summaries/    章节总结
RAG/settings/                   世界观和术语
```

### 前端

文件：[public/js/resources.js](public/js/resources.js)

- `loadRagTree()`：读取并刷新目录树；
- `renderTree()`：递归生成左侧资源管理器；
- `openFile()`：打开资料文件；
- 保存按钮：把右侧编辑内容写回 RAG。

如要增加删除、重命名或移动文件功能，需要同时扩展 rag.js 后端接口和 resources.js 前端操作。

## 6. 生图功能

### 前端

文件：[public/js/image.js](public/js/image.js)

- 「优化提示词」请求 `/api/image/optimize`；
- 「生成图片」优先使用优化后的提示词，否则使用原始提示词；
- 成功后将服务器返回的图片 URL 放入结果区域。

### 后端

文件：[server/routes/image.js](server/routes/image.js)

| 接口 | 用途 |
| --- | --- |
| `POST /api/image/optimize` | 使用写作模型优化图片提示词 |
| `POST /api/image/generate` | 调用生图模型生成图片 |

具体生图请求位于 [server/services/imageService.js](server/services/imageService.js)。该服务兼容接口返回 `b64_json` 或远程 `url`，最终统一保存到 `public/generated/`。

若生图服务商的参数不同，主要修改 `generateImage()` 中的请求体和返回解析逻辑。

## 7. 前端公共代码

文件：[public/js/app.js](public/js/app.js)

提供三个公共工具：

- `api()`：统一发送 JSON 请求并处理错误；
- `toast()`：显示成功或错误提示；
- `withLoading()`：管理按钮请求中的加载状态。

新增前端接口调用时，建议继续使用这三个方法，保持交互和错误处理一致。

## 8. 常见修改位置

| 修改目标 | 主要文件 |
| --- | --- |
| 修改界面布局 | public/index.html |
| 修改纸质风格、颜色、尺寸 | public/css/style.css |
| 修改写作交互 | public/js/writing.js |
| 修改 AI 默认提示词 | server/routes/ai.js 或对应 SKILL.md |
| 修改 AI 接口格式 | server/services/llm.js |
| 修改资料分类 | RAG/ 目录及 public/js/resources.js |
| 增加资料文件操作 | server/routes/rag.js + public/js/resources.js |
| 修改生图参数或返回解析 | server/services/imageService.js |
| 更换模型、密钥或端口 | .env |

## 9. 修改后的基本检查

完成代码修改后，至少检查：

1. `npm start` 能正常启动；
2. 首页和三个导航页可打开；
3. 写作内容刷新后仍能恢复；
4. 普通归稿会追加到阅读区末尾，召回编辑会替换回原位置，且刷新后仍可继续确认替换；
5. AI 提示、补全、总结接口可返回结果；
6. 总结成功后，章节正文和章节总结分别出现在两个资料目录；
7. 资料文件只能在 RAG 目录内读写；
8. 生图失败时前端能显示服务器错误信息；
9. 同步更新 [迭代记录.md](迭代记录.md) 和必要的 [readme.md](readme.md)。
