# 技术文档

> 功能与使用方法见 [readme.md](readme.md)，历史变化见 [迭代记录.md](迭代记录.md)。

## 1. 架构

- 后端：Node.js + Express 4、CommonJS
- 前端：原生 HTML/CSS/JavaScript
- AI：Chat Completions 兼容接口
- 生图：Images Generations 兼容接口
- 资料：`RAG/<book>/` 下 Markdown 文件
- 写作状态：`data/writing/<book>.json`
- 提示词：`skills/<name>/SKILL.md` 纯文本伪 skill

服务器入口 [server/index.js](server/index.js) 挂载 `/api/ai`、`/api/image`、`/api/rag`、`/api/writing`。启动时调用 `migrateLegacyStorage()` 完成旧版单书数据的保留式迁移。

## 2. 多书籍存储

核心模块：[server/services/bookStorage.js](server/services/bookStorage.js)

固定目录：

```text
RAG/<book>/
  characters/
  plots/volumes/
  plots/chapter-bodies/
  plots/chapter-summaries/
  settings/
```

关键函数：

- `validateBookName()`：拒绝空名称、`.`、`..`、控制字符、路径分隔符及 Windows 非法字符；
- `resolveBookRoot()` / `resolveBookPath()`：用 `path.resolve()` 组合路径，并验证最终位置仍在指定书籍根目录内；
- `ensureBookStructure()`：创建标准目录；
- `listBooks()` / `createBook()`：书籍列表与创建；
- `buildChapterTitle()`：扫描章节文件并生成下一章标题；
- `getChapterSummaries()` / `getVolumes()`：按文件名解析章节和卷元数据；
- `atomicWrite()`：先写临时文件再重命名，用于保护术语文件；
- `migrateLegacyStorage()`：将旧 RAG 根目录和 `data/writing.json` 迁移到 `example`。

迁移冲突规则：同名同内容删除重复源文件；同名不同内容以 `（迁移冲突）` 后缀保留源文件。旧 `plots/chapters/` 按章节总结处理。

## 3. 当前书籍前端上下文

[public/js/app.js](public/js/app.js) 负责：

- `GET /api/rag/books` 加载书籍列表；
- 首选 `localStorage.novel-current-book`，否则选择 `example` 或第一本书；
- `getCurrentBook()` 暴露当前书籍；
- 切换前调用 `window.flushWritingSave()` 保存原书工作区；
- 派发 `books-ready` 和 `book-changed` 事件，写作页与资料页据此刷新；
- `POST /api/rag/books` 创建书籍。

## 4. RAG 接口

路由：[server/routes/rag.js](server/routes/rag.js)

| 接口 | 说明 |
| --- | --- |
| `GET /api/rag/books` | 列出书籍 |
| `POST /api/rag/books` | 创建书籍及标准目录 |
| `GET /api/rag/tree?book=...` | 获取指定书籍资源树 |
| `GET /api/rag/file?book=...&path=...` | 读取书内相对路径文件 |
| `POST /api/rag/file` | 保存 `{book, path, content}` |

所有文件 API 都先验证书籍，再通过 `resolveBookPath()` 限制在 `RAG/<book>/` 内。

## 5. 按书写作状态

路由：[server/routes/writing.js](server/routes/writing.js)

```js
{
  paragraphs: [],
  draft: '',
  chapter: '',
  activeRecall: null
}
```

- `GET /api/writing?book=...` 读取 `data/writing/<book>.json`；
- `POST /api/writing` 保存带 `book` 的状态；
- `activeRecall` 继续保存原段落快照、选区段落索引、段内偏移和原草稿；
- [public/js/writing.js](public/js/writing.js) 在切换书籍后重置内存，再读取目标工作区，避免串稿；
- `replaceRangeInParagraphs()` 仍负责单段和跨段原位替换。

## 6. 伪 skill

加载器：[server/services/skillLoader.js](server/services/skillLoader.js)

`SKILL.md` 可以保留 frontmatter，`stripFrontmatter()` 只返回正文。`requirePrompt(name)` 要求正文非空，否则抛出明确错误；代码不再含 fallback 提示词。

| 功能 | 伪 skill |
| --- | --- |
| 写作提示 | `writing-hint` |
| AI 补全 | `writing-complete` |
| 章节总结与术语 | `chapter-summary` |
| 分卷总结 | `volume-summary` |
| 图片提示词优化 | `image-prompt-optimize` |

这些文件仅作为 `chat(system, user)` 的 system prompt，不是标准 Agent Skill。

## 7. 章节总结与自动编号

路由：[server/routes/ai.js](server/routes/ai.js)

`POST /api/ai/summarize` 请求：

```json
{ "book": "example", "content": "章节正文", "chapter": "可选标题" }
```

流程：

1. 扫描 `chapter-bodies` 和 `chapter-summaries` 中符合 `第N章...md` 的文件，兼容阿拉伯数字和中文数字编号，取最大编号加一；
2. 清除用户标题中可能已有的章节前缀和非法文件名字符；
3. 空标题生成 `第N章`，非空生成 `第N章：标题`；
4. 先保存完整正文；
5. 读取 `settings/术语.md`，与本章正文一起调用 `chapter-summary`；
6. 用 `【章节总结】` 和 `【更新后术语】` 解析纯文本；
7. 成功后写章节总结，并原子替换完整术语文档。

解析失败时不写总结、不覆盖术语，但步骤 4 的正文保留。成功响应包含 `summary`、`chapterNumber`、`chapterTitle`、`bodyPath`、`summaryPath`、`terminologyPath`。

`chapter-summary` 将章节剧情与长期术语严格分离。章节标题、章节日志、剧情复盘、规则和关键事件不得进入术语文档；人物只保留稳定身份、关系、长期特征及极简必要事实，其他实体只在属于本书特有且后续写作需要依赖的设定时收录。普通常识、临时状态和一般实体不收录。若本章没有可靠新增或修正，模型必须在 `【更新后术语】` 区段原样返回已有术语；该区段要求完整输出，但不代表必须产生变更。

## 8. 分卷总结

接口：

| 接口 | 说明 |
| --- | --- |
| `GET /api/ai/volume-summary/meta?book=...` | 返回下一卷号、已有卷范围和章节列表 |
| `POST /api/ai/volume-summary` | 校验并生成分卷总结 |

生成请求：

```json
{ "book": "example", "volumeNumber": 2, "chapters": [13, 14, 15] }
```

服务器权威校验：

- 卷号等于已有最大卷号加一，无卷时为 1；旧版 `第一卷.md` 等中文卷号会参与下一卷计算，并作为历史上下文读取，但因没有章节范围而不参与章节重叠判断；
- 至少选择一个章节；
- 章节无重复、按升序、编号连续；
- 章节总结文件全部存在；
- 章节范围不与已有卷重叠。

通过后读取所有历史卷全文和本卷章节总结，调用 `volume-summary`，保存为 `plots/volumes/第N卷：起-止.md`。前端弹窗位于 [public/index.html](public/index.html)，交互位于 [public/js/resources.js](public/js/resources.js)。

## 9. 生图

[server/routes/image.js](server/routes/image.js) 的 `/optimize` 使用 `requirePrompt('image-prompt-optimize')`；`/generate` 调用 [server/services/imageService.js](server/services/imageService.js)。生成图片保存在 `public/generated/`。

## 10. 修改后检查

1. 对修改过的 JavaScript 执行 `node --check`；
2. 确认旧资料全部位于 `RAG/example/`，旧写作状态位于 `data/writing/example.json`；
3. 验证新建和切换书籍后写作状态、资料树互不串联；
4. 验证空标题和自定义标题的自动章节编号；
5. 验证章节总结成功更新术语，错误输出不破坏旧术语；
6. 验证错误卷号、非连续章节和重复范围被拒绝；
7. 验证构造 `book` 或 `path` 不能越过书籍根目录；
8. 同步更新四份项目文档。
